# This file is now optional and only needed for local development tools (e.g., Drizzle migrations).
# All runtime config and secrets are managed in wrangler.toml ([vars]) and via Wrangler secrets.
# Do not store deployment or production secrets here.
